<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_splash_bg_color"
    tools:context=".activity.SplashScreenActivity">

    <ImageView
        android:id="@+id/splash_pict"
        android:layout_width="230dp"
        android:layout_height="230dp"
        android:layout_marginBottom="200dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:rotation="-20"
        android:background="@drawable/img_cinema"/>
    <TextView
        android:id="@+id/lol"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        app:layout_constraintStart_toStartOf="@id/splash_pict"
        app:layout_constraintEnd_toEndOf="@id/splash_pict"
        app:layout_constraintTop_toBottomOf="@id/splash_pict"
        android:text="@string/splash_screen_desc"
        android:textAlignment="center"
        android:letterSpacing="0.2"
        android:fontFamily="@font/black_casper"
        android:textSize="40sp"
        android:textColor="@color/gold"/>

</androidx.constraintlayout.widget.ConstraintLayout>